# Training data

Per-model training sets: `X.npy` (inputs), `y.npy` (labels), and a `.csv`
mirror for inspection. One set per model in the hierarchy — engine off,
engine start, the three load levels, behaviour, and the routers.

Synthetic and fully reproducible: rebuild with `notebooks/DataGeneration/`.
