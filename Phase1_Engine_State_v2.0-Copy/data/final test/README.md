# Final test data

A second, independent evaluation set. Every model is scored here as well as
on test/ — consistent results across both indicate the accuracy is real and
not the product of a lucky split.

Synthetic and fully reproducible: rebuild with notebooks/DataGeneration/.
