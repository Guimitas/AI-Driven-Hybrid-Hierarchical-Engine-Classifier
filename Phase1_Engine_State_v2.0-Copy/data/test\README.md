# Test data

Held-out split used to evaluate each model after training. Same format and
generators as the training data, drawn separately so no sample is shared.

Synthetic and fully reproducible: rebuild with notebooks/DataGeneration/.
