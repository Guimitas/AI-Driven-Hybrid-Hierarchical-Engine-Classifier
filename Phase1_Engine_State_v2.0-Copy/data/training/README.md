# Training data

   Per-model training sets: X.npy (inputs), y.npy (labels), and a .csv
   mirror for inspection. One set per model in the hierarchy.

   Synthetic and fully reproducible: rebuild with notebooks/DataGeneration/.
